{
  "$schema": "https://playground.wordpress.org/blueprint-schema.json",
  "landingPage": "/shop/",
  "preferredVersions": {
    "php": "8.1",
    "wp": "latest"
  },
  "features": {
    "networking": true
  },
  "steps": [
    {
      "step": "login",
      "username": "admin",
      "password": "password"
    },
    {
      "step": "installPlugin",
      "pluginData": {
        "resource": "wordpress.org/plugins",
        "slug": "woocommerce"
      },
      "options": {
        "activate": true
      }
    },
    {
      "step": "installTheme",
      "themeData": {
        "resource": "wordpress.org/themes",
        "slug": "twentytwentyfive"
      }
    },
    {
      "step": "installTheme",
      "themeData": {
        "resource": "git:directory",
        "url": "https://github.com/darlingteatime/darlingteatime-theme.git",
        "ref": "main",
        "path": "/"
      },
      "options": {
        "activate": true
      }
    },
    {
      "step": "runPHP",
      "code": "<?php\nrequire_once '/wordpress/wp-load.php';\n// Run the seeder dynamically inside the active theme directory\n$seeder_file = get_stylesheet_directory() . '/bin/seed-products.php';\nif ( file_exists( $seeder_file ) ) {\n    require_once $seeder_file;\n} else {\n    echo 'Warning: Seeder file not found at ' . $seeder_file . '\\n';\n}\n"
    },
    {
      "step": "runPHP",
      "code": "<?php\nrequire_once '/wordpress/wp-load.php';\n// Enable pretty permalinks inside the WASM sandbox\nglobal $wp_rewrite;\n$wp_rewrite->set_permalink_structure('/%postname%/');\n$wp_rewrite->flush_rules(true);\n"
    }
  ]
}
